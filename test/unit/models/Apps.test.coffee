describe 'AppsModel', ->
  describe '#find()', ->
    it 'should check Apps.find function', (done) ->
      Apps.find().then((results) ->
        # some tests
        
        done()
        return
      ).catch done
      return
    return
  return